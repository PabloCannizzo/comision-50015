<div class="d-flex justify-content-center align-items-center">
    <h1 class="text-center">Productos</h1>
</div>
<div class="tienda">
    <div class="intro-productos">
        <h1>Encontra y descubri toda nuestra variedad en juguetes ideales para los mas pequeños y la familia</h1>
    </div>

    <div class="productos">
        {{#each productos}}
        <div class="producto">
            <img src="{{img}}" alt="" class="img">
            <div class="card">
                <h2>{{title}}</h2>
                <p>{{description}}</p>
                <h3>Precio: $ {{price}}</h3>
                <p>Stock: {{stock}}</p>
                <form action="/api/carts/{{../cartId}}/product/{{id}}" method="POST">
                    <div class="input-group mb-3">
                        <button type="button" class="btn btn-outline-secondary"
                            onclick="decreaseQuantity(this)">-</button>
                        <input type="number" class="form-control" value="1" name="quantity" id="quantityInput" readonly>
                        <button type="button" class="btn btn-outline-secondary"
                            onclick="increaseQuantity(this)">+</button>
                    </div>
                    <button class="btnMios" type="submit">Agregar al Carrito</button>
                </form>
            </div>
        </div>
        {{/each}}
    </div>

    <div class="selec-pag">
        <div>
            <p>Pagina {{currentPage}} de {{totalPages}} </p>
        </div>
        {{#if hasNextPage}}
        <a href="/products?page={{nextPage}}"> Siguiente </a>
        {{/if}}
        {{#if hasPrevPage}}
        <a href="/products?page={{prevPage}}"> Anterior </a>
        {{/if}}
    </div>
</div>

<script>
    function decreaseQuantity(button) {
        let input = button.parentNode.querySelector('input[type=number]');
        let currentValue = parseInt(input.value);
        if (!isNaN(currentValue) && currentValue > 1) {
            input.value = currentValue - 1;
        }
    }

    function increaseQuantity(button) {
        let input = button.parentNode.querySelector('input[type=number]');
        let currentValue = parseInt(input.value);
        if (!isNaN(currentValue)) {
            input.value = currentValue + 1;
        }
    }
</script>

{{!-- <p>CartID: {{cartId}}</p> --}}