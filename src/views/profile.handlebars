<h1>Mi Perfil</h1>

<div class="profile-card">
	<p>Bienvenido, {{user.nombre}} {{user.last_name}}!</p>
	<p>Rol: {{user.role}}</p>

	<form action="/api/users/logout" method="POST">
		<button class="btnMios" type="submit">Logout</button>
	</form>

	{{#if this.isAdmin}}
	<a class="btnMios" href="/realtimeproducts">Modificar Productos</a>
	<a class="btnMios" href="/users">Ver Usuarios</a>
	{{else}}
	{{#if this.isPremium}}
	<a class="btnMios" href="/realtimeproducts">Panel Premium</a>
	{{/if}}
	<a class="btnMios" href="/chat">Chat en vivo</a>
	<a class="btnMios" href="/products">Tienda</a>
	{{/if}}
	{{!-- <div>
		<h1>
			Queres cambiarte a premium y vender tus propios productos, hace click en el siguiente enlace
		</h1>
		<a href="/pre-premium"> hacete premium</a>
	</div> --}}
</div>